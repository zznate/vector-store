<?xml version="1.0"?>
<!DOCTYPE module PUBLIC
  "-//Checkstyle//DTD Checkstyle Configuration 1.3//EN"
  "https://checkstyle.org/dtds/configuration_1_3.dtd">

<!--
  Focused Checkstyle ruleset for vector-store. Complements PMD: Checkstyle
  enforces lexical / style consistency (imports, braces, naming, line length),
  PMD enforces complexity / correctness. Together they cover the project's
  engineering-standards memory without needing the full Google or Sun
  rulesets, which are too noisy for this codebase.

  Manual invocation:
    ./mvnw checkstyle:check
-->
<module name="Checker">
  <property name="severity" value="error"/>
  <property name="charset" value="UTF-8"/>
  <property name="fileExtensions" value="java"/>

  <!-- Exclude target/ generated sources. -->
  <module name="BeforeExecutionExclusionFileFilter">
    <property name="fileNamePattern" value=".*[\\/]target[\\/].*"/>
  </module>

  <!-- File-level checks. -->
  <module name="LineLength">
    <property name="max" value="120"/>
    <!-- Don't break on long imports or URLs in Javadoc. -->
    <property name="ignorePattern"
              value="^package.*|^import.*|a href|href|http://|https://|ftp://"/>
  </module>
  <module name="FileTabCharacter">
    <property name="eachLine" value="true"/>
  </module>
  <module name="NewlineAtEndOfFile"/>

  <module name="TreeWalker">

    <!-- Imports. -->
    <module name="UnusedImports"/>
    <module name="RedundantImport"/>
    <module name="AvoidStarImport"/>

    <!-- Braces and whitespace. -->
    <module name="LeftCurly"/>
    <module name="RightCurly"/>
    <module name="NeedBraces"/>
    <module name="WhitespaceAfter"/>

    <!-- Class structure. -->
    <module name="OneTopLevelClass"/>

    <!-- Naming. -->
    <module name="PackageName">
      <property name="format" value="^[a-z]+(\.[a-z][a-z0-9]*)*$"/>
    </module>
    <module name="TypeName"/>
    <module name="MethodName"/>
    <module name="ConstantName"/>
    <module name="LocalVariableName"/>
    <module name="MemberName"/>
    <module name="ParameterName"/>
    <module name="StaticVariableName"/>

    <!-- Annotations. -->
    <module name="MissingOverride"/>

    <!-- Misc correctness. -->
    <module name="EmptyStatement"/>
    <module name="EqualsHashCode"/>
    <module name="UpperEll"/>
    <module name="ArrayTypeStyle"/>
    <module name="DefaultComesLast"/>
    <module name="FallThrough"/>

  </module>
</module>
